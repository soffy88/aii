tunnel: 24b59c9e-b7e0-483d-9b13-52d68582cf38
ingress:
  - hostname: aii-api.uex.hk
    service: http://localhost:8101
  - hostname: aii.uex.hk
    service: http://localhost:3101
  - service: http_status:404
# NOTE: this tunnel runs with `--token` → ingress is managed in the Cloudflare
# dashboard, NOT this file. To proxy WebSocket, add a Public Hostname route in the
# dashboard: aii.uex.hk  path=/ws  → http://localhost:9304 (before the :3101 rule).
